export enum Routes {
  Root = '/',
  Login = '/login',
  PasswordHealth = '/items',
  Weak = '/items/weak',
  Reused = '/items/reused',
  Old = '/items/old',
}

export enum API {
  Login = 'api/login',
  Items = 'api/items',
  User = 'api/user',
}
